import styled from 'styled-components';
import * as COLOR from 'globalStyled/app-colors';

const Container = styled.nav`
  display: flex;
  background-color: ${COLOR.BACKGROUND_COLOR_1};
  margin-top: 0.5px;
`;

export { Container };
