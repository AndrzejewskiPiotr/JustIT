import React, { FunctionComponent } from 'react';

import { Container } from './bottomNavigation-styled';

const BottomNavigation: FunctionComponent = (): JSX.Element => <Container />;

export default BottomNavigation;
